const Like = require('../models/likeModel')
const sequelize = require('../config/mySql-config')

const like= async()=>{
    try {
        
    } catch (error) {
        throw error        
    }
}

const dislike = async()=>{
    try {
        
    } catch (error) {
        throw error
    }
}

module.exports={
    like, 
    dislike
}